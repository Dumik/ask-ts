import { ContainerHomePage } from 'components';
import { PublicLayout } from 'layouts';

export const HomePage = () => {
  return (
    <PublicLayout>
      <ContainerHomePage />
    </PublicLayout>
  );
};
